```markdown
# Контроллеры

REST-контроллеры для управления пользователями, картами и переводами.

## 📁 Структура контроллеров

```
src/main/java/com/example/bankcards/controller/
├── UserController.java          # Управление пользователями
├── CardController.java          # Управление банковскими картами
└── AuthController.java          # Аутентификация и авторизация
```

## 👥 UserController

**Путь:** `/api/v1/users`
**Доступ:** Аутентифицированные пользователи с ролевой моделью

### Методы

#### 🔐 Только ADMIN
- `POST /` - Создание нового пользователя
- `GET /` - Получение списка всех пользователей (пагинация)
- `GET /{userId}` - Получение пользователя по ID
- `PATCH /{userId}/role` - Изменение роли пользователя
- `DELETE /{userId}` - Удаление пользователя

#### 👤 Для всех аутентифицированных
- `GET /me` - Получение информации о текущем пользователе
- `PATCH /{userId}/username` - Изменение имени пользователя

### Аннотации безопасности
```java
@PreAuthorize("hasRole('ADMIN')")    // Только администраторы
@PreAuthorize("isAuthenticated()")   // Все аутентифицированные
```

## 💳 CardController

**Путь:** `/api/v1/cards`
**Доступ:** Управление картами с разделением прав

### Методы

#### 🔐 Только ADMIN
- `POST /user/{userId}` - Создание карты для пользователя
- `GET /admin` - Получение всех карт (фильтрация + пагинация)
- `PATCH /{cardId}/block` - Блокировка карты
- `PATCH /{cardId}/activate` - Активация карты
- `DELETE /{cardId}` - Удаление карты

#### 👤 Операции пользователя
- `GET /my` - Получение своих карт (фильтрация + пагинация)
- `GET /{cardId}/balance` - Получение баланса карты
- `PATCH /my/{cardId}/block` - Блокировка своей карты
- `POST /transfer` - Перевод между своими картами

### Фильтрация и пагинация
**Параметры:**
- `ownerName` - фильтр по имени владельца
- `status` - фильтр по статусу (ACTIVE, BLOCKED, EXPIRED)
- `page` - номер страницы (по умолчанию 0)
- `size` - размер страницы (по умолчанию 10)

## 🔐 AuthController

**Путь:** `/api/v1/auth`
**Доступ:** Публичный доступ

### Методы
- `POST /login` - Аутентификация пользователя и получение JWT токена
- `POST /register` - Регистрация нового пользователя (если предусмотрено)
- `POST /refresh` - Обновление JWT токена

## 🛡️ Безопасность

Все контроллеры используют Spring Security с JWT аутентификацией:
- Автоматическая проверка прав доступа
- Валидация входных данных
- Защита от распространенных уязвимостей
- Маскирование конфиденциальных данных

## 📋 Ответы API

**Успешные ответы:**
- `200 OK` - операция выполнена успешно
- `201 Created` - ресурс создан
- `204 No Content` - ресурс удален

**Ошибки:**
- `400 Bad Request` - невалидные данные
- `401 Unauthorized` - требуется аутентификация
- `403 Forbidden` - недостаточно прав
- `404 Not Found` - ресурс не найден
- `409 Conflict` - конфликт данных